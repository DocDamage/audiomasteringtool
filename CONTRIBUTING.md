# Contributing

## Baseline

- C++20
- CMake 3.24+
- warnings enabled
- no generated/build artifacts committed
- tests required for domain behavior
- no production model weights committed directly to Git
- dependency/model licenses must be recorded before merge

## Branches

Use focused feature branches and pull requests for implementation work after the Phase 0 baseline.
