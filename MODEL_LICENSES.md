# Model licences

No production model weights are distributed in Phase 0.

Every future model must have an entry in `models/registry.json` containing separate code/weight licensing, commercial-use status, artifact hash, provenance, resource requirements, and security-review status before it can enter a release.
